import jwt from 'jsonwebtoken';

export default class AutheticationHelper
{
    authenticationToke = async (token) => {
        try {
            
        } catch (error) {
            
        }
    }
}